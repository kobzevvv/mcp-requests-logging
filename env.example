# Cloudflare Worker → BigQuery ingestion
# Rename to .env for local development or configure via Wrangler secrets.

# Optional HMAC verification. If unset, signature verification is disabled.
WEBHOOK_SECRET=

# BigQuery destination
BIGQUERY_PROJECT_ID=
BIGQUERY_DATASET=hiring_router_mcp
BIGQUERY_TABLE=logging_events

# GCP service account (from a JSON key)
# If using Workers Secrets, store the full key as JSON and parse, or store fields separately.
GCP_CLIENT_EMAIL=
GCP_PRIVATE_KEY=

# Optional: override producer/source identifiers if needed by your environment
SOURCE_NAME=hiring_router_mcp
SCHEMA_VERSION=1
